import 'package:dart_language/built-in-types/records.dart';

void main() {
  // var json = {'name': 'Alice', 'age': 30};
  // var info = userInfo(json);
  // var name = info.item1; // 访问元组的第一个元素
  // var age = info.item2; // 访问元组的第二个元素

  // print('Name: $name, Age: $age');
}
